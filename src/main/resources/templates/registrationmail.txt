Hey [( ${data.participantName} )],

You have successfully registered to wichtel at '[( ${data.title} )]' ([( ${domain} )]/about)!
You'll be provided with the name of your gift's recipient through an email to [( ${data.participantEmail} )] once everybody has registered.
The gift's monetary value should not exceed [( ${data.price} )].

Here's what the event's host says about it:

"[( ${data.description} )]"

If you have any questions, contact the event's host [( ${data.host.name} )] at [( ${data.host.email} )].

This mail was generated using [( ${domain} )]